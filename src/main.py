"""
MMUD — Mesh Multi-User Dungeon
Main entry point. Connects to Meshtastic device and runs the game loop.
"""

import sys


def main():
    print("MMUD server starting...")
    print("TODO: Phase 1 implementation")
    # 1. Initialize SQLite database
    # 2. Connect to Meshtastic device
    # 3. Enter message processing loop:
    #    receive_message() → parse_command() → check_action_budget()
    #    → execute_action() → format_response() → send_message()


if __name__ == "__main__":
    main()
